# docs — Documentation Index

Reference documentation for FlowBoard. Every file here describes **what shipped**,
verified against the code — not what was planned. If a doc and the code disagree,
the code is the authority and the doc is the bug; fix it in the same change.

| File                                         | What's inside                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | When to read                                                                                                                  |
| -------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| [architecture.md](./architecture.md)         | The monorepo map and the turbo task graph (why `typecheck`/`test` need `^build`, why `e2e#test` runs last and uncached); the request lifecycle end to end — `validate(schema, part)` + the typed `getParsed` accessor, `asyncHandler`, `respond`, `ApiError` and the single `errorHandler`; the `routes → controllers → services → db` rule **and its three documented exceptions** (`sockets/socket-reads.ts`; `bootstrap.ts` with its four injected sinks plus `registerRealtimeBridge` / `registerNotificationSubscribers`; and `middlewares/require-roles.ts`, the guards), plus the `admin-logs` controller-without-a-service; the domain-events bus and who publishes vs subscribes; and prose walkthroughs of a Kanban drag and a login. | Before anything structural, before adding a package, or when you need to know where a boundary actually is.                   |
| [coding-standards.md](./coding-standards.md) | The naming table; the no-`any` gate plus `noUncheckedIndexedAccess` and the per-workspace `verbatimModuleSyntax` split; zod at both ends; CommonJS conventions in `apps/api`; the `Executor = Db \| Tx` pattern and **the trio every mutation owes** (`recordActivity` inside the transaction, `record()` + `publishDomainEvent()` after it); soft-delete discipline and `pg-errors.ts`; the test mechanics (jsdom pragmas, the `MemoryStorage` shim in `test/setup.ts`, `test-db.ts`, `fileParallelism`); the complete **`fb-*-v1` storage-key registry**; and LF + Prettier.                                                                                                                                                                  | Every time you write code. Jump to the trio section before writing any service mutation.                                      |
| [design-system.md](./design-system.md)       | The `index.css` layer order and the shadcn bridge (`@theme inline`, and the `--color-accent` → `--sidebar-active` naming trap); the **22 colour tokens + 23 shared tokens** of the zod theme document; the Theme Studio (eight colour presets, eight font presets with Arabic fallback, `applyTheme()` pre-mount, the OKLCH helpers, import/export, the live favicon); density and dimension tokens; the `--chart-*` roles and the task-type icons that ride the same ramp; the **closed hex-literal exemption table**; the 26 frozen `components/ui/*` primitives; and the Linear-minimal principles.                                                                                                                                          | Any styling work, any token change, or before adding a `components/ui/*` primitive.                                           |
| [database.md](./database.md)                 | The 22-table map; Drizzle conventions — uuid PKs vs bigserial append-only streams, the seven pg enums vs the workflow data tables, `columns.ts`'s shared factories, the six soft-delete tables vs user deactivation; the dual fractional-rank columns and the rebalance rule; all **16 check constraints** and the **40 indexes**; the single `0000_initial_schema.sql` migration, its journal, and the `pg_trgm` hand-edit that drizzle-kit cannot emit; the four scripts (and why `db:generate` re-implements the dotenv chain); and exactly what the seed produces.                                                                                                                                                                          | Any schema or query change — always alongside [workflows/db-migration.md](../workflows/db-migration.md).                      |
| [auth.md](./auth.md)                         | The access/refresh token pair, claims, rotation, and **every site that bumps or checks `tokenVersion`**; the three-member `AuthProvider` interface (`id`, `supportsPasswordChange`, `verifyCredentials`) and a step-by-step recipe for a future `LdapAuthProvider`; a 15-row endpoint table across `/api/auth`, org invites, and admin users; the invite lifecycle including what still previews after expiry; the role-resolution chain and `res.locals.orgAccess` / `projectAccess`; socket-handshake revocation; and the client's single-flight refresh.                                                                                                                                                                                     | Auth endpoints, guards, invites, or anything permission-shaped.                                                               |
| [i18n.md](./i18n.md)                         | The two layers (`lib/lang-policy.ts` owning `fb-lang-v1` and the `<html lang\|dir>` stamp; `src/i18n/` owning i18next) and the one-way dependency; the boot order; the 19 typed TS namespaces; the **binding Arabic glossary**; the plural policy and exactly what `locales.test.ts` does and does not assert; RTL mechanics and `Direction.Provider` (plus sonner's second `dir` pass); the **complete physical-property exception table**; the LTR islands (Gantt axis, `ChartFrame`, the diagnostics dock); the `dir="auto"` convention; Western digits; the validation/error-code seams; and the add-a-string recipe.                                                                                                                       | **Any user-facing string**, and any layout work. The single most-read doc in this tree.                                       |
| [diagnostics.md](./diagnostics.md)           | The pino ring buffer contract (capacity, monotonic ids from 1, process-local); `GET /api/admin/logs?sinceId` and why it is polled rather than pushed; the drawer's dock math, poll store, restart-rewind, stick-to-bottom, and copy-as-JSONL; the `Ctrl+J` / `Ctrl+Shift+J` chords (the second **cycles** the dock, it is not an alternate); a 21-row testid table; and the drawer's storage keys.                                                                                                                                                                                                                                                                                                                                              | Log-viewer work, or adding a log line you expect to see in the drawer.                                                        |
| [telemetry.md](./telemetry.md)               | The **12-event closed enum** with an emission-site table; the server-authoritative / client-emitted split (exactly three client types: `page_view`, `theme_changed`, `export_csv`) and the `POST /api/telemetry/events` ingest contract and what the server refuses to trust; `record()`'s fire-and-forget semantics and its injected sink; the batched `request_logs` pipeline and route-pattern normalization; the five `/api/admin/telemetry/*` endpoints; and the math decisions — `percentile_cont`, half-open buckets, **5xx-only `errorRate`**.                                                                                                                                                                                          | Emitting an event, or building/altering an analytics chart.                                                                   |
| [realtime.md](./realtime.md)                 | The **14-event map** (3 C→S, 11 S→C) with payloads, emitters, and target rooms; `user:{userId}` and membership-checked `project:{projectId}` rooms; **echo suppression** end to end (`X-Socket-Id` → `originSocketId` → `io.to(room).except(...)`); the bridge's hydration via `socket-reads.ts` and its **parse-before-emit** rule; presence (socket-lifetime, no heartbeat, no TTL); the web **cache-write mapping table**; and reconnect semantics plus the `rebalanced` refetch flag.                                                                                                                                                                                                                                                       | Wiring a socket event, touching presence, or writing any optimistic mutation.                                                 |
| [testing.md](./testing.md)                   | The six-layer pyramid and what each layer owns — **and must not do**; how to run every suite, and the definitive answer on infrastructure (`vitest.config.ts` sets `DATABASE_URL` itself; 26 of the API's 41 files need Postgres on 5433, the web suites need nothing, MinIO is never needed); the fixture and harness catalogue (four supertest app builders, the real socket harness, the web render harnesses); the **152-file / ~2 900-case spec inventory** by area; and determinism, including `flushAsync()` for fire-and-forget writes.                                                                                                                                                                                                 | Before writing or running tests, or when a suite fails. The mechanics (pragmas, cleanup, config) live in coding-standards.md. |

See the master [.agents/INDEX.md](../INDEX.md).
