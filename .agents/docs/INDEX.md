# docs — Documentation Index

Reference documentation for FlowBoard. Every file here describes **what shipped**,
verified against the code — not what was planned. If a doc and the code disagree,
the code is the authority and the doc is the bug; fix it in the same change.

| File                                         | What's inside                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | When to read                                                                                                                  |
| -------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| [architecture.md](./architecture.md)         | The monorepo map and the turbo task graph (why `typecheck`/`test` need `^build`, why `e2e#test` runs last and uncached); the request lifecycle end to end — `validate(schema, part)` + the typed `getParsed` accessor, `asyncHandler`, `respond`, `ApiError` and the single `errorHandler`; the `routes → controllers → services → db` rule **and its three documented exceptions**, plus the `admin-logs` controller-without-a-service; the Round-2 routers and their mount order; the domain-events bus; the nav data model; the **one sanctioned place server data lives in Zustand**; and prose walkthroughs of a Kanban drag and a login.                                                                                                           | Before anything structural, before adding a package, or when you need to know where a boundary actually is.                   |
| [coding-standards.md](./coding-standards.md) | The naming table; the no-`any` gate plus `noUncheckedIndexedAccess` and the per-workspace `verbatimModuleSyntax` split; zod at both ends; **shared chrome reads the catalog in one module** (the borrow table); CommonJS conventions in `apps/api`; the `Executor = Db \| Tx` pattern and **the trio every mutation owes**; soft-delete discipline and `pg-errors.ts`; the test mechanics (jsdom pragmas, the `MemoryStorage` shim, `test-db.ts`, `fileParallelism`); the complete **`fb-*-v1` storage-key registry**; and LF + Prettier.                                                                                                                                                                                                                | Every time you write code. Jump to the trio section before writing any service mutation.                                      |
| [design-system.md](./design-system.md)       | The `index.css` layer order — including the **unlayered motion gate that must stay last** — and the shadcn bridge (`@theme inline`, the `--color-accent` → `--sidebar-active` naming trap); the 22 colour tokens + 23 shared tokens of the zod theme document; the Theme Studio (eight colour presets, eight font presets, `applyTheme()` pre-mount, OKLCH helpers, import/export, the live favicon) **and its drawer**; density and dimension tokens; the `--chart-*` roles; the **closed hex-literal exemption table**; the 33 frozen `components/ui/*` primitives; and **§10, the dashboard primitive kit** — `PanelCard` vs `ReportCard`, the three range vocabularies, `DataTable` on TanStack v9, `useGridUrlState`, the chrome-copy borrow table. | Any styling work, any token change, before adding a `components/ui/*` primitive, or before building a console page.           |
| [database.md](./database.md)                 | The 23-table map; Drizzle conventions — uuid PKs vs bigserial append-only streams, the seven pg enums vs the workflow data tables, `columns.ts`'s shared factories, the six soft-delete tables vs user **anonymization**; the dual fractional-rank columns and the rebalance rule; all **17 check constraints** and the **41 indexes**; the **two migrations** and the two hand-edits drizzle-kit cannot emit; the four scripts; and exactly what the two-org seed produces.                                                                                                                                                                                                                                                                             | Any schema or query change — always alongside [workflows/db-migration.md](../workflows/db-migration.md).                      |
| [auth.md](./auth.md)                         | The access/refresh token pair, claims, rotation, and **every site that bumps or checks `tokenVersion`**; the three-member `AuthProvider` interface and a recipe for a future `LdapAuthProvider`; a 15-row endpoint table across `/api/auth`, org invites, and admin users; the invite lifecycle; the role-resolution chain and `res.locals.orgAccess` / `projectAccess`; socket-handshake revocation; and the client's single-flight refresh.                                                                                                                                                                                                                                                                                                            | Auth endpoints, guards, invites, or anything permission-shaped.                                                               |
| [admin.md](./admin.md)                       | Everything above an organization: the `instance_settings` singleton, **single-org mode** and every site that collapses for it; the three instance endpoints and what `PATCH` refuses; the nav data model (`nav.config.ts` → sidebar, breadcrumbs, palette) and **the four escape routes**; **view-as-member** — real vs effective admin, the bounce rule, `?scope=member`; the five console pages and every capability the users console exposes; **anonymize-delete**, field by field; and org archive/restore.                                                                                                                                                                                                                                         | Anything under `/admin`, any instance-level setting, or a change to how `/` resolves.                                         |
| [analytics.md](./analytics.md)               | The console's five doctrine rules — **tile-is-the-link**, header-only Details, **no per-metric endpoints**, one domain call + client projection, a metric is data; the five domain endpoints and their window contract (`MAX_BUCKETS = 400`); the metric registry — `defineMetric`, `MetricDefinition`, the twenty metrics, `Object.hasOwn` lookups; the `metric-catalog.ts` key layer and `AnalyticsKey`; the promise-keyed domain cache, the shared range store and **cold vs warm**; the drill-down page; the chart predicates and the bucket-caption rule; and the two open rough edges.                                                                                                                                                             | Anything under `/admin/analytics`, or before adding a number to an admin surface.                                             |
| [i18n.md](./i18n.md)                         | The two layers (`lib/lang-policy.ts` owning `fb-lang-v1` and the `<html lang\|dir>` stamp; `src/i18n/` owning i18next) and the one-way dependency; the boot order; the **20 typed TS namespaces**; **typed-literal key config modules** (hand-written `NavLabelKey` vs derived `AnalyticsKey`); the **binding Arabic glossary**; the plural policy and exactly what `locales.test.ts` does and does not assert; RTL mechanics, `Direction.Provider`, and the drawer's behavioural arrow flip; the **complete physical-property exception table**; the LTR islands (Gantt axis, `ChartFrame`, the diagnostics dock, `StatDelta`, endpoint paths); `dir="auto"`; Western digits; the validation/error-code seams; and the add-a-string recipe.             | **Any user-facing string**, and any layout work. The single most-read doc in this tree.                                       |
| [motion.md](./motion.md)                     | The doctrine (chrome moves at `--speed`, nothing else animates); `lib/motion-policy.ts` and why **`full` beats a reducing OS**; the `<html data-motion>` stamp and the **unlayered `:where()` gate layer**, including why `animate-spin` is the one exception; the **six-entry closed registry** and what each entry owes; the two enforcement seams — the `motion` import grep test and the self-enforcing CSS; which durations are tokenized and which argue for themselves; and the recipe for adding an entry.                                                                                                                                                                                                                                       | Before making anything move, importing the `motion` library, or touching the bottom of `index.css`.                           |
| [diagnostics.md](./diagnostics.md)           | The pino ring buffer contract (capacity, monotonic ids from 1, process-local); `GET /api/admin/logs?sinceId` and why it is polled rather than pushed; the drawer's dock math, poll store, restart-rewind, stick-to-bottom, and copy-as-JSONL; the `Ctrl+J` / `Ctrl+Shift+J` chords (the second **cycles** the dock); a 21-row testid table; and the drawer's storage keys.                                                                                                                                                                                                                                                                                                                                                                               | Log-viewer work, or adding a log line you expect to see in the drawer.                                                        |
| [telemetry.md](./telemetry.md)               | The **12-event closed enum** with an emission-site table; the server-authoritative / client-emitted split and the `POST /api/telemetry/events` ingest contract; `record()`'s fire-and-forget semantics and its injected sink; the batched `request_logs` pipeline and route-pattern normalization; the five `/api/admin/telemetry/*` endpoints **and how the analytics family differs from them**; and the math — `percentile_cont`, half-open buckets, **5xx-only `errorRate`**.                                                                                                                                                                                                                                                                        | Emitting an event, or building/altering an ops chart.                                                                         |
| [realtime.md](./realtime.md)                 | The **14-event map** (3 C→S, 11 S→C) with payloads, emitters, and target rooms; `user:{userId}` and membership-checked `project:{projectId}` rooms; **echo suppression** end to end (`X-Socket-Id` → `originSocketId` → `io.to(room).except(...)`); the bridge's hydration via `socket-reads.ts` and its **parse-before-emit** rule; presence; the web **cache-write mapping table**; and reconnect semantics plus the `rebalanced` refetch flag.                                                                                                                                                                                                                                                                                                        | Wiring a socket event, touching presence, or writing any optimistic mutation.                                                 |
| [testing.md](./testing.md)                   | The six-layer pyramid and what each layer owns — **and must not do**; how to run every suite, and the definitive answer on infrastructure (`vitest.config.ts` sets `DATABASE_URL` itself; **31 of the API's 46 files** need Postgres on 5433, the web suites need nothing, MinIO is never needed); the fixture and harness catalogue, including **why a grid suite needs `BrowserRouter`**; the spec **area map** (a command, not a stale total); the two **enforcement suites**; and determinism, including `flushAsync()` for fire-and-forget writes.                                                                                                                                                                                                  | Before writing or running tests, or when a suite fails. The mechanics (pragmas, cleanup, config) live in coding-standards.md. |

See the master [.agents/INDEX.md](../INDEX.md).
